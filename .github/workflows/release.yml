name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: SpencerRWood/workflows/.github/workflows/release.yml@v1
    secrets:
      github_token: ${{ secrets.GITHUB_TOKEN }}
